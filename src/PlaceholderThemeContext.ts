import React from 'react';
import { PlaceholderStyleProps } from './PlaceholderStyleProps.js';

/**
 * @internal
 */
export const PlaceholderThemeContext = React.createContext<PlaceholderStyleProps>({});
